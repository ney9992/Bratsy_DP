---
phase: 01-nastrojki-i-konfiguraciya
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01-PLAN.md"
files_modified:
  - app/create_test.ps1
autonomous: true
requirements:
  - UI-02
  - INT-03

must_haves:
  truths:
    - "Пользователь нажимает '…' рядом с полем .spp — открывается OpenFileDialog с фильтром *.spp"
    - "Пользователь нажимает '…' рядом с рабочим каталогом — открывается FolderBrowserDialog"
    - "Пользователь нажимает '…' рядом с папкой скриптов — открывается FolderBrowserDialog"
    - "Нажатие Сохранить с несуществующим путём: красный бордер на TextBox + подпись 'Путь не найден' под полем"
    - "Нажатие Сохранить с валидными путями (или пустыми): settings.json записывается в папку .exe, панель закрывается"
    - "При запуске приложения settings.json читается, TextBox'ы заполняются сохранёнными значениями"
  artifacts:
    - path: "app/create_test.ps1"
      provides: "Три поля TextBox+Button для выбора путей в settingsPanel"
      contains: "$tbPlantSimPath"
    - path: "app/create_test.ps1"
      provides: "Цвета ошибки"
      contains: "$colErrorBg"
    - path: "app/create_test.ps1"
      provides: "Сохранение настроек в JSON"
      contains: "ConvertTo-Json"
    - path: "app/create_test.ps1"
      provides: "Загрузка настроек при старте"
      contains: "ConvertFrom-Json"
  key_links:
    - from: "$saveBtn.Add_Click"
      to: "settings.json"
      via: "ConvertTo-Json | Out-File -Encoding UTF8"
      pattern: "ConvertTo-Json.*Out-File"
    - from: "$form.Add_Load"
      to: "$tbPlantSimPath.Text"
      via: "ConvertFrom-Json из settings.json"
      pattern: "ConvertFrom-Json"
    - from: "Validate-SettingsPath"
      to: "$tbPlantSimPath"
      via: "Test-Path + красный бордер через Add_Paint"
      pattern: "colErrorBg|colErrorText"
---

<objective>
Добавить в $settingsPanel (созданный в Plan 01) три поля выбора путей с диалогами, логику валидации, сохранения в settings.json и загрузки при старте.

Purpose: Реализует требования UI-02 (поля настроек) и INT-03 (путь к .spp) — без этих данных интеграция с Plant Simulation невозможна.
Output: Три поля TextBox+Button внутри $settingsPanel, валидация с визуальной индикацией ошибок, сохранение/загрузка через settings.json.
</objective>

<execution_context>
@E:/Bratsy_DP/.claude/get-shit-done/workflows/execute-plan.md
@E:/Bratsy_DP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/Bratsy_DP/.planning/ROADMAP.md
@E:/Bratsy_DP/.planning/phases/01-nastrojki-i-konfiguraciya/01-CONTEXT.md
@E:/Bratsy_DP/.planning/phases/01-nastrojki-i-konfiguraciya/01-01-SUMMARY.md

<interfaces>
<!-- Паттерны из create_test.ps1, актуальные для этого плана -->

Существующая цветовая палитра (расширить двумя новыми цветами ошибки):
```powershell
# Существующие:
$colSuccessBg    = [System.Drawing.Color]::FromArgb(225, 245, 238)
$colSuccessText  = [System.Drawing.Color]::FromArgb(15, 110, 86)
# Новые (добавить в секцию Color palette по аналогии):
$colErrorBg      = [System.Drawing.Color]::FromArgb(253, 235, 236)
$colErrorText    = [System.Drawing.Color]::FromArgb(180, 35, 45)
```

Паттерн FlatStyle TextBox (ReadOnly) + кнопка "…":
```powershell
# TextBox — ReadOnly, т.к. путь выбирается диалогом
$tb = New-Object System.Windows.Forms.TextBox
$tb.ReadOnly = $true
$tb.BackColor = $colBgPage
$tb.ForeColor = $colTextPrimary
$tb.BorderStyle = "FixedSingle"
$tb.Font = New-Object System.Drawing.Font("Segoe UI", 9)

# Кнопка "…" — FlatStyle по аналогии с $runBtn
$browseBtn = New-Object System.Windows.Forms.Button
$browseBtn.Text = "..."
$browseBtn.FlatStyle = "Flat"
$browseBtn.FlatAppearance.BorderColor = $colBorder
$browseBtn.FlatAppearance.BorderSize = 1
$browseBtn.Cursor = [System.Windows.Forms.Cursors]::Hand
```

Паттерн $card.Controls.Add — все контролы полей добавляются в $settingsPanel, не в $card напрямую.

$settingsPanel уже создан в Plan 01. Поля добавляются в него после заголовка (y > 56px) и до футера (y < 500px).

Логика $form.Add_Load для загрузки — вставить ДО `[void]$form.ShowDialog()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить цвета ошибки и три поля TextBox+Button в $settingsPanel</name>
  <read_first>
    - app/create_test.ps1 (секция Color palette строки 9-22; блок $settingsPanel из Plan 01)
    - .planning/phases/01-nastrojki-i-konfiguraciya/01-CONTEXT.md (решения D-05, D-06)
    - .planning/phases/01-nastrojki-i-konfiguraciya/01-01-SUMMARY.md (что именно создано в Plan 01)
  </read_first>
  <files>app/create_test.ps1</files>
  <action>
**Шаг A — Добавить цвета ошибки** в секцию `# ===== Color palette =====` сразу после `$colMutedText`:
```powershell
$colErrorBg      = [System.Drawing.Color]::FromArgb(253, 235, 236)
$colErrorText    = [System.Drawing.Color]::FromArgb(180, 35, 45)
```

**Шаг B — Добавить вспомогательную функцию** `Add-SettingsField` для создания поля (метка + TextBox + кнопка "…" + метка ошибки). Функцию добавить после блока `Get-LogoImage`, до `# ===== Form =====`:

```powershell
# ===== Settings field helper =====
function Add-SettingsField {
    param(
        [System.Windows.Forms.Panel]$parent,
        [string]$labelText,
        [int]$yPos,
        [string]$placeholder,
        [string]$dialogType   # "file" or "folder"
    )

    $fieldW = 310  # ширина TextBox

    $lbl = New-Object System.Windows.Forms.Label
    $lbl.Text = $labelText
    $lbl.Location = New-Object System.Drawing.Point(20, $yPos)
    $lbl.Size = New-Object System.Drawing.Size($fieldW, 18)
    $lbl.Font = New-Object System.Drawing.Font("Segoe UI", 8.5)
    $lbl.ForeColor = $colTextSec
    $lbl.BackColor = $colBgCard
    $parent.Controls.Add($lbl)

    $tb = New-Object System.Windows.Forms.TextBox
    $tb.ReadOnly = $true
    $tb.Location = New-Object System.Drawing.Point(20, ($yPos + 22))
    $tb.Size = New-Object System.Drawing.Size(270, 24)
    $tb.BackColor = $colBgPage
    $tb.ForeColor = $colTextPrimary
    $tb.BorderStyle = "FixedSingle"
    $tb.Font = New-Object System.Drawing.Font("Segoe UI", 9)
    $tb.Text = $placeholder
    $parent.Controls.Add($tb)

    # Метка ошибки (скрыта по умолчанию)
    $errLbl = New-Object System.Windows.Forms.Label
    $errLbl.Text = "Путь не найден"
    $errLbl.Location = New-Object System.Drawing.Point(20, ($yPos + 50))
    $errLbl.Size = New-Object System.Drawing.Size($fieldW, 16)
    $errLbl.Font = New-Object System.Drawing.Font("Segoe UI", 8)
    $errLbl.ForeColor = $colErrorText
    $errLbl.BackColor = $colBgCard
    $errLbl.Visible = $false
    $parent.Controls.Add($errLbl)

    $browseBtn = New-Object System.Windows.Forms.Button
    $browseBtn.Text = "..."
    $browseBtn.Location = New-Object System.Drawing.Point(296, ($yPos + 20))
    $browseBtn.Size = New-Object System.Drawing.Size(34, 28)
    $browseBtn.FlatStyle = "Flat"
    $browseBtn.FlatAppearance.BorderColor = $colBorder
    $browseBtn.FlatAppearance.BorderSize = 1
    $browseBtn.Font = New-Object System.Drawing.Font("Segoe UI", 9)
    $browseBtn.Cursor = [System.Windows.Forms.Cursors]::Hand
    $browseBtn.BackColor = $colBgCard
    $browseBtn.ForeColor = $colTextPrimary

    if ($dialogType -eq "file") {
        $browseBtn.Add_Click({
            $dlg = New-Object System.Windows.Forms.OpenFileDialog
            $dlg.Filter = "Plant Simulation files (*.spp)|*.spp|All files (*.*)|*.*"
            $dlg.Title = "Выберите файл Plant Simulation"
            if ($dlg.ShowDialog() -eq "OK") {
                $tb.Text = $dlg.FileName
                $tb.BackColor = $colBgPage
                $errLbl.Visible = $false
            }
        }.GetNewClosure())
    } else {
        $browseBtn.Add_Click({
            $dlg = New-Object System.Windows.Forms.FolderBrowserDialog
            $dlg.Description = "Выберите папку"
            if ($dlg.ShowDialog() -eq "OK") {
                $tb.Text = $dlg.SelectedPath
                $tb.BackColor = $colBgPage
                $errLbl.Visible = $false
            }
        }.GetNewClosure())
    }
    $parent.Controls.Add($browseBtn)

    return [PSCustomObject]@{ TextBox = $tb; ErrorLabel = $errLbl }
}
```

**Шаг C — Добавить три поля в $settingsPanel** (после блока создания $settingsPanel из Plan 01, до $settingsTimer):

```powershell
# ===== Settings fields =====
$fieldPlantSim = Add-SettingsField -parent $settingsPanel -labelText "Путь к файлу Plant Simulation (.spp)" -yPos 76  -placeholder "" -dialogType "file"
$fieldWorkDir  = Add-SettingsField -parent $settingsPanel -labelText "Рабочий каталог"                       -yPos 160 -placeholder "" -dialogType "folder"
$fieldScripts  = Add-SettingsField -parent $settingsPanel -labelText "Папка со скриптами / макросами"        -yPos 244 -placeholder "" -dialogType "folder"

$tbPlantSimPath = $fieldPlantSim.TextBox
$tbWorkDir      = $fieldWorkDir.TextBox
$tbScriptsDir   = $fieldScripts.TextBox
```

Поля размещены с вертикальным шагом 84px (22px метка + 24px поле + 16px ошибка + 22px отступ): y=76, y=160, y=244. Это укладывается в диапазон 56..500px (после хедер-дивайдера, до футер-дивайдера).
  </action>
  <verify>
    <automated>Select-String -Path "E:/Bratsy_DP/app/create_test.ps1" -Pattern 'colErrorBg|tbPlantSimPath|Add-SettingsField|FolderBrowserDialog|OpenFileDialog' | Select-Object -ExpandProperty Pattern | Sort-Object -Unique | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - Файл содержит `$colErrorBg = [System.Drawing.Color]::FromArgb(253, 235, 236)`
    - Файл содержит `$colErrorText = [System.Drawing.Color]::FromArgb(180, 35, 45)`
    - Файл содержит функцию `function Add-SettingsField`
    - Файл содержит `$tbPlantSimPath`, `$tbWorkDir`, `$tbScriptsDir`
    - Файл содержит `OpenFileDialog` с фильтром `*.spp`
    - Файл содержит два вызова `FolderBrowserDialog` (рабочий каталог + папка скриптов)
    - Поля добавляются в `$settingsPanel`, не в `$card` или `$form`
    - Функция `Add-SettingsField` возвращает объект с полями `TextBox` и `ErrorLabel`
    - `$tb.ReadOnly = $true` у всех TextBox'ов полей путей
  </acceptance_criteria>
  <done>Три поля (TextBox ReadOnly + кнопка "…") добавлены в $settingsPanel. OpenFileDialog с *.spp для Plant Simulation, FolderBrowserDialog для двух папок. Цвета ошибки $colErrorBg / $colErrorText определены.</done>
</task>

<task type="auto">
  <name>Task 2: Валидация, сохранение в settings.json и загрузка при старте</name>
  <read_first>
    - app/create_test.ps1 (блок $saveBtn из Plan 01; переменные $tbPlantSimPath из Task 1; строка `[void]$form.ShowDialog()`)
    - .planning/phases/01-nastrojki-i-konfiguraciya/01-CONTEXT.md (решения D-07, D-08, D-09, D-10, D-11, D-12)
  </read_first>
  <files>app/create_test.ps1</files>
  <action>
**Шаг A — Привязать $saveBtn.Add_Click** (найти `$saveBtn` из Plan 01, добавить обработчик клика):

```powershell
$saveBtn.Add_Click({
    $hasError = $false

    # Валидация: Test-Path для непустых полей (D-10, D-11, D-12)
    foreach ($field in @($fieldPlantSim, $fieldWorkDir, $fieldScripts)) {
        $tb  = $field.TextBox
        $err = $field.ErrorLabel
        if ($tb.Text -ne "" -and -not (Test-Path $tb.Text)) {
            $tb.BackColor = $colErrorBg
            $err.Visible  = $true
            $hasError      = $true
        } else {
            $tb.BackColor = $colBgPage
            $err.Visible  = $false
        }
    }

    if ($hasError) { return }

    # Сохранение (D-07, D-09)
    $settingsPath = Join-Path $scriptDir "settings.json"
    $cfg = [PSCustomObject]@{
        PlantSimPath = $tbPlantSimPath.Text
        WorkDir      = $tbWorkDir.Text
        ScriptsDir   = $tbScriptsDir.Text
    }
    $cfg | ConvertTo-Json | Out-File -FilePath $settingsPath -Encoding UTF8

    # Закрыть панель анимацией
    $script:settingsClosing = $true
    $settingsTimer.Start()
}.GetNewClosure())
```

**Шаг B — Загрузка при старте** (D-08). Добавить блок `$form.Add_Load` ДО строки `[void]$form.ShowDialog()`:

```powershell
# ===== Load settings on startup =====
$form.Add_Load({
    $settingsPath = Join-Path $scriptDir "settings.json"
    if (Test-Path $settingsPath) {
        try {
            $cfg = Get-Content $settingsPath -Raw -Encoding UTF8 | ConvertFrom-Json
            if ($cfg.PlantSimPath) { $tbPlantSimPath.Text = $cfg.PlantSimPath }
            if ($cfg.WorkDir)      { $tbWorkDir.Text      = $cfg.WorkDir }
            if ($cfg.ScriptsDir)   { $tbScriptsDir.Text   = $cfg.ScriptsDir }
        } catch {
            # Corrupt JSON — ignore, fields stay empty
        }
    }
})
```

Порядок вставки в файле:
1. `$saveBtn.Add_Click` — после создания `$saveBtn` (которое в Plan 01) и после `$fieldPlantSim`/`$fieldWorkDir`/`$fieldScripts` (Task 1)
2. `$form.Add_Load` — последний блок перед `[void]$form.ShowDialog()`
  </action>
  <verify>
    <automated>Select-String -Path "E:/Bratsy_DP/app/create_test.ps1" -Pattern 'ConvertTo-Json|ConvertFrom-Json|settings\.json|form\.Add_Load|hasError' | Select-Object -ExpandProperty Pattern | Sort-Object -Unique | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - Файл содержит `ConvertTo-Json` — запись настроек
    - Файл содержит `ConvertFrom-Json` — загрузка настроек
    - Файл содержит `settings.json` (путь через `Join-Path $scriptDir "settings.json"`)
    - Файл содержит `$form.Add_Load` с блоком загрузки настроек
    - Файл содержит `$hasError = $false` — переменная управления валидацией
    - Файл содержит `Test-Path $tb.Text` — проверка существования пути
    - Файл содержит `$tb.BackColor = $colErrorBg` — визуальная индикация ошибки
    - Файл содержит `$err.Visible = $true` — показ метки "Путь не найден"
    - Блок `$form.Add_Load` расположен ПЕРЕД строкой `[void]$form.ShowDialog()`
    - Сохранение использует `-Encoding UTF8` (требование D-09)
    - Валидация срабатывает только при клике Save, не on-blur (D-10)
    - Пустые поля считаются валидными (D-12): условие `$tb.Text -ne "" -and -not (Test-Path...)`
  </acceptance_criteria>
  <done>Клик "Сохранить" валидирует непустые пути через Test-Path; невалидные поля получают $colErrorBg фон и метку "Путь не найден". При успехе — settings.json (PlantSimPath, WorkDir, ScriptsDir) записывается в $PSScriptRoot, панель закрывается анимацией. При следующем запуске приложения settings.json читается и TextBox'ы заполняются.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Файловая система → settings.json | Файл читается из $PSScriptRoot (рядом с .exe); содержимое — только пути |
| settings.json → TextBox.Text | Строки из JSON попадают в ReadOnly TextBox'ы; не выполняются как код |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | settings.json | accept | Файл содержит только пути к ресурсам, не credentials. Ручное редактирование файла — в рамках прав заводского пользователя. |
| T-02-02 | Information Disclosure | settings.json | accept | Пути к файлам не являются секретами в заводской среде; файл локальный. |
| T-02-03 | Elevation of Privilege | OpenFileDialog/FolderBrowserDialog | mitigate | Диалоги открываются в контексте текущего пользователя; пути попадают в ReadOnly TextBox и не передаются в Invoke-Expression или аналоги. При сохранении используется Out-File (не Invoke-Expression). |
| T-02-04 | Denial of Service | ConvertFrom-Json | mitigate | Обёрнуть в try/catch (уже в плане): повреждённый JSON игнорируется, поля остаются пустыми, приложение не падает. |
</threat_model>

<verification>
Запустить скрипт: `powershell -ExecutionPolicy Bypass -File "E:/Bratsy_DP/app/create_test.ps1"`

Проверить сценарии:

**Сценарий 1 — Выбор пути к .spp:**
1. Клик ⚙ → панель выезжает
2. Клик "…" у поля "Путь к файлу Plant Simulation" → открывается OpenFileDialog с фильтром "Plant Simulation files (*.spp)"
3. Выбрать или отменить — TextBox обновляется / остаётся пустым

**Сценарий 2 — Выбор папок:**
1. Клик "…" у "Рабочий каталог" → FolderBrowserDialog
2. Клик "…" у "Папка со скриптами / макросами" → FolderBrowserDialog

**Сценарий 3 — Валидация с несуществующим путём:**
1. Вручную (через PowerShell до запуска GUI) создать settings.json с `"PlantSimPath": "C:\\nonexistent\\file.spp"`
2. Запустить приложение — TextBox заполнен несуществующим путём
3. Клик "Сохранить" → поле PlantSimPath краснеет, видна подпись "Путь не найден"
4. Панель НЕ закрывается

**Сценарий 4 — Сохранение и перезапуск:**
1. Выбрать существующую папку через FolderBrowserDialog для WorkDir
2. Клик "Сохранить" → панель закрывается
3. В $PSScriptRoot проверить: `Get-Content settings.json` — WorkDir содержит выбранный путь
4. Перезапустить скрипт → поле WorkDir уже заполнено

**Сценарий 5 — Пустые поля:**
1. Все поля пусты → Сохранить → settings.json создаётся с пустыми строками, ошибок нет
</verification>

<success_criteria>
- Три поля (Путь к .spp, Рабочий каталог, Папка со скриптами) отображаются в $settingsPanel
- OpenFileDialog с фильтром *.spp открывается для первого поля
- FolderBrowserDialog открывается для второго и третьего полей
- Несуществующий непустой путь при Save → $colErrorBg на TextBox + "Путь не найден" под полем
- Пустые поля при Save → валидны, settings.json записывается
- settings.json содержит ключи PlantSimPath, WorkDir, ScriptsDir в UTF-8
- При следующем запуске приложения TextBox'ы заполнены из settings.json
- Скрипт запускается без ошибок PowerShell
</success_criteria>

<output>
После завершения создать `.planning/phases/01-nastrojki-i-konfiguraciya/01-02-SUMMARY.md`
</output>
