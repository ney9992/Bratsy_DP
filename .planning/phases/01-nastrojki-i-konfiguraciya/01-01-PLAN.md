---
phase: 01-nastrojki-i-konfiguraciya
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/create_test.ps1
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "Кнопка ⚙ видна в хедере при запуске приложения (~x=1080, y=22)"
    - "Клик по кнопке ⚙ открывает боковую панель, выезжающую справа"
    - "Панель выезжает плавно (Timer-анимация), не появляется мгновенно"
    - "Панель перекрывает карточки этапов, окно не расширяется"
    - "Повторный клик по ⚙ (или кнопка Отмена) закрывает панель с обратной анимацией"
    - "Панель имеет заголовок 'Настройки' и футер с кнопками Сохранить / Отмена"
  artifacts:
    - path: "app/create_test.ps1"
      provides: "Gear button + sliding settings panel shell"
      contains: "$gearBtn"
    - path: "app/create_test.ps1"
      provides: "Settings panel Timer animation"
      contains: "$settingsTimer"
    - path: "app/create_test.ps1"
      provides: "Settings panel Panel control"
      contains: "$settingsPanel"
  key_links:
    - from: "$gearBtn.Add_Click"
      to: "$settingsTimer"
      via: "Timer.Start() to animate panel open/close"
      pattern: "settingsTimer.*Start"
    - from: "$settingsTimer.Add_Tick"
      to: "$settingsPanel.Width"
      via: "incrementing Width from 0 to 350 (open) or 350 to 0 (close)"
      pattern: "settingsPanel.*Width"
---

<objective>
Добавить в существующий UI-прототип (app/create_test.ps1) кнопку-иконку ⚙ в правый угол хедера и боковую панель настроек с анимацией выезда справа.

Purpose: Пользователь должен иметь точку входа в настройки — без этой панели сохранение конфигурации невозможно (требование UI-02).
Output: Модифицированный app/create_test.ps1 с кнопкой ⚙, анимированной боковой панелью ($settingsPanel), заголовком панели и футером с кнопками Сохранить / Отмена.
</objective>

<execution_context>
@E:/Bratsy_DP/.claude/get-shit-done/workflows/execute-plan.md
@E:/Bratsy_DP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/Bratsy_DP/.planning/PROJECT.md
@E:/Bratsy_DP/.planning/ROADMAP.md
@E:/Bratsy_DP/.planning/phases/01-nastrojki-i-konfiguraciya/01-CONTEXT.md

<interfaces>
<!-- Ключевые переменные и паттерны из app/create_test.ps1, которые ОБЯЗАТЕЛЬНО переиспользуются -->

Цветовая палитра (строки 9-22 create_test.ps1):
```powershell
$colBgPage       = [System.Drawing.Color]::FromArgb(245, 244, 240)
$colBgCard       = [System.Drawing.Color]::White
$colBorder       = [System.Drawing.Color]::FromArgb(220, 218, 210)
$colBorderInfo   = [System.Drawing.Color]::FromArgb(23, 95, 165)
$colTextPrimary  = [System.Drawing.Color]::FromArgb(40, 40, 38)
$colTextSec      = [System.Drawing.Color]::FromArgb(120, 119, 112)
$colTextTert     = [System.Drawing.Color]::FromArgb(160, 158, 150)
$colSuccessBg    = [System.Drawing.Color]::FromArgb(225, 245, 238)
$colSuccessText  = [System.Drawing.Color]::FromArgb(15, 110, 86)
$colInfoBg       = [System.Drawing.Color]::FromArgb(230, 241, 251)
$colInfoText     = [System.Drawing.Color]::FromArgb(12, 68, 124)
$colMutedBg      = [System.Drawing.Color]::FromArgb(241, 239, 232)
$colMutedText    = [System.Drawing.Color]::FromArgb(95, 94, 90)
```

Паттерн headerIcon (строки 74-89) — ТОЧНО ТАК ЖЕ строится $gearBtn:
```powershell
$headerIcon = New-Object System.Windows.Forms.Panel
$headerIcon.Location = New-Object System.Drawing.Point(24, 22)
$headerIcon.Size = New-Object System.Drawing.Size(40, 40)
$headerIcon.BackColor = $colInfoBg
$headerIcon.Add_Paint({
    $g = $_.Graphics
    $g.SmoothingMode = "AntiAlias"
    $g.TextRenderingHint = "AntiAlias"
    $f = New-Object System.Drawing.Font("Segoe UI", 18, [System.Drawing.FontStyle]::Bold)
    $brush = New-Object System.Drawing.SolidBrush $colInfoText
    $sf = New-Object System.Drawing.StringFormat
    $sf.Alignment = "Center"
    $sf.LineAlignment = "Center"
    $g.DrawString([char]0x2302, $f, $brush, (New-Object System.Drawing.RectangleF 0, 0, 40, 40), $sf)
})
$card.Controls.Add($headerIcon)
```

Паттерн FlatStyle кнопки (строки 397-407):
```powershell
$runBtn = New-Object System.Windows.Forms.Button
$runBtn.FlatStyle = "Flat"
$runBtn.FlatAppearance.BorderColor = $colBorder
$runBtn.FlatAppearance.BorderSize = 1
$runBtn.Font = New-Object System.Drawing.Font("Segoe UI", 9.5)
$runBtn.Cursor = [System.Windows.Forms.Cursors]::Hand
```

Паттерн CardTimer (строки 240-277) — ТОЧНАЯ СТРУКТУРА для $settingsTimer:
```powershell
$cardTimer = New-Object System.Windows.Forms.Timer
$cardTimer.Interval = 12
$cardTimer.Add_Tick({
    param($sender, $e)
    $c = $sender.Tag
    # ... изменяет Size карточки пошагово
    if ($curW -eq $targetW -and $curH -eq $targetH) {
        $sender.Stop()
        return
    }
    if ($curW -lt $targetW) { $curW = [Math]::Min($targetW, $curW + $c.Step) }
    ...
    $c.Size = New-Object System.Drawing.Size($curW, $curH)
})
$cardTimer.Tag = $stageCard
```

Существующий $statusPanel (строка 111): Location = Point(960, 28), Size = Size(180, 22)
Фиксированный размер окна: $form.Size = Size(1200, 620), $card.Size = Size(1160, 560)
Все контролы добавляются в $card, не в $form.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить кнопку-иконку ⚙ в хедер</name>
  <read_first>
    - app/create_test.ps1 (весь файл — уже прочитан, паттерны в секции interfaces выше)
    - .planning/phases/01-nastrojki-i-konfiguraciya/01-CONTEXT.md (решения D-01, D-02)
  </read_first>
  <files>app/create_test.ps1</files>
  <action>
После блока `$statusPanel` (строка ~123, `$card.Controls.Add($statusPanel)`) добавить кнопку-иконку шестерёнки. Кнопка реализуется как Panel с Add_Paint — точно по паттерну $headerIcon.

Конкретный код для вставки (после `$card.Controls.Add($statusPanel)`):

```powershell
# ===== Gear button (settings trigger) =====
$script:settingsPanelOpen = $false

$gearBtn = New-Object System.Windows.Forms.Panel
$gearBtn.Location = New-Object System.Drawing.Point(1080, 22)
$gearBtn.Size = New-Object System.Drawing.Size(40, 40)
$gearBtn.BackColor = $colBgCard
$gearBtn.Cursor = [System.Windows.Forms.Cursors]::Hand
$gearBtn.Add_Paint({
    $g = $_.Graphics
    $g.SmoothingMode = "AntiAlias"
    $g.TextRenderingHint = "AntiAlias"
    $f = New-Object System.Drawing.Font("Segoe UI", 18)
    $brush = New-Object System.Drawing.SolidBrush $colTextSec
    $sf = New-Object System.Drawing.StringFormat
    $sf.Alignment = "Center"
    $sf.LineAlignment = "Center"
    $g.DrawString([char]0x2699, $f, $brush, (New-Object System.Drawing.RectangleF 0, 0, 40, 40), $sf)
})
$gearBtn.Add_Click({
    if ($script:settingsPanelOpen) {
        $script:settingsClosing = $true
        $settingsTimer.Start()
    } else {
        $script:settingsClosing = $false
        $settingsPanel.Width = 0
        $settingsPanel.Visible = $true
        $settingsPanel.BringToFront()
        $settingsTimer.Start()
    }
})
$card.Controls.Add($gearBtn)
```

Переменная `$script:settingsPanelOpen` и `$script:settingsClosing` используются в Task 2.
  </action>
  <verify>
    <automated>Select-String -Path "E:/Bratsy_DP/app/create_test.ps1" -Pattern '\$gearBtn' | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - Файл содержит строку `$gearBtn = New-Object System.Windows.Forms.Panel`
    - Файл содержит строку `[char]0x2699` (символ шестерёнки)
    - Файл содержит `$gearBtn.Location = New-Object System.Drawing.Point(1080, 22)`
    - Файл содержит `$card.Controls.Add($gearBtn)`
    - Файл содержит `$script:settingsPanelOpen = $false`
    - Кнопка НЕ добавляется в `$form` напрямую — только в `$card`
  </acceptance_criteria>
  <done>Кнопка ⚙ добавлена в $card на позицию (1080, 22), рисует [char]0x2699 через Add_Paint, клик вызывает логику открытия/закрытия панели.</done>
</task>

<task type="auto">
  <name>Task 2: Создать боковую панель настроек с Timer-анимацией</name>
  <read_first>
    - app/create_test.ps1 (паттерн CardTimer, строки 240-277 — уже в interfaces выше)
    - .planning/phases/01-nastrojki-i-konfiguraciya/01-CONTEXT.md (решения D-02, D-03, D-04)
  </read_first>
  <files>app/create_test.ps1</files>
  <action>
После блока $gearBtn (Task 1) добавить $settingsPanel и $settingsTimer. Вставить ДО строки `[void]$form.ShowDialog()`.

Конкретный код:

```powershell
# ===== Settings Panel =====
$settingsPanel = New-Object System.Windows.Forms.Panel
$settingsPanel.Location = New-Object System.Drawing.Point(810, 0)
$settingsPanel.Size = New-Object System.Drawing.Size(0, 560)
$settingsPanel.BackColor = $colBgCard
$settingsPanel.Visible = $false
$settingsPanel.Add_Paint({
    $g = $_.Graphics
    $g.SmoothingMode = "AntiAlias"
    $pen = New-Object System.Drawing.Pen $colBorder, 1
    $g.DrawRectangle($pen, 0, 0, $settingsPanel.Width - 1, $settingsPanel.Height - 1)
})
$card.Controls.Add($settingsPanel)

# Settings panel header
$settingsTitleLbl = New-Object System.Windows.Forms.Label
$settingsTitleLbl.Text = "Настройки"
$settingsTitleLbl.Location = New-Object System.Drawing.Point(20, 20)
$settingsTitleLbl.Size = New-Object System.Drawing.Size(260, 26)
$settingsTitleLbl.Font = New-Object System.Drawing.Font("Segoe UI Semibold", 12)
$settingsTitleLbl.ForeColor = $colTextPrimary
$settingsTitleLbl.BackColor = $colBgCard
$settingsPanel.Controls.Add($settingsTitleLbl)

$settingsDivider = New-Object System.Windows.Forms.Panel
$settingsDivider.Location = New-Object System.Drawing.Point(0, 56)
$settingsDivider.Size = New-Object System.Drawing.Size(350, 1)
$settingsDivider.BackColor = $colBorder
$settingsPanel.Controls.Add($settingsDivider)

# Footer divider
$settingsFootDivider = New-Object System.Windows.Forms.Panel
$settingsFootDivider.Location = New-Object System.Drawing.Point(0, 500)
$settingsFootDivider.Size = New-Object System.Drawing.Size(350, 1)
$settingsFootDivider.BackColor = $colBorder
$settingsPanel.Controls.Add($settingsFootDivider)

# Save button
$saveBtn = New-Object System.Windows.Forms.Button
$saveBtn.Text = "Сохранить"
$saveBtn.Location = New-Object System.Drawing.Point(20, 512)
$saveBtn.Size = New-Object System.Drawing.Size(140, 34)
$saveBtn.BackColor = $colBgCard
$saveBtn.ForeColor = $colTextPrimary
$saveBtn.FlatStyle = "Flat"
$saveBtn.FlatAppearance.BorderColor = $colBorder
$saveBtn.FlatAppearance.BorderSize = 1
$saveBtn.Font = New-Object System.Drawing.Font("Segoe UI", 9.5)
$saveBtn.Cursor = [System.Windows.Forms.Cursors]::Hand
$settingsPanel.Controls.Add($saveBtn)

# Cancel button
$cancelBtn = New-Object System.Windows.Forms.Button
$cancelBtn.Text = "Отмена"
$cancelBtn.Location = New-Object System.Drawing.Point(176, 512)
$cancelBtn.Size = New-Object System.Drawing.Size(140, 34)
$cancelBtn.BackColor = $colBgCard
$cancelBtn.ForeColor = $colTextSec
$cancelBtn.FlatStyle = "Flat"
$cancelBtn.FlatAppearance.BorderColor = $colBorder
$cancelBtn.FlatAppearance.BorderSize = 1
$cancelBtn.Font = New-Object System.Drawing.Font("Segoe UI", 9.5)
$cancelBtn.Cursor = [System.Windows.Forms.Cursors]::Hand
$cancelBtn.Add_Click({
    $script:settingsClosing = $true
    $settingsTimer.Start()
})
$settingsPanel.Controls.Add($cancelBtn)

# Settings slide animation timer (same structure as CardTimer)
$settingsPanelTargetW = 350
$settingsAnimStep = 10

$settingsTimer = New-Object System.Windows.Forms.Timer
$settingsTimer.Interval = 12
$settingsTimer.Add_Tick({
    if ($script:settingsClosing) {
        $newW = [Math]::Max(0, $settingsPanel.Width - $settingsAnimStep)
        $settingsPanel.Width = $newW
        $settingsPanel.Location = New-Object System.Drawing.Point((1160 - $newW), 0)
        if ($newW -eq 0) {
            $settingsTimer.Stop()
            $settingsPanel.Visible = $false
            $script:settingsPanelOpen = $false
        }
    } else {
        $newW = [Math]::Min($settingsPanelTargetW, $settingsPanel.Width + $settingsAnimStep)
        $settingsPanel.Width = $newW
        $settingsPanel.Location = New-Object System.Drawing.Point((1160 - $newW), 0)
        if ($newW -eq $settingsPanelTargetW) {
            $settingsTimer.Stop()
            $script:settingsPanelOpen = $true
        }
    }
}.GetNewClosure())
```

Важно: `$settingsPanel.Location` пересчитывается на каждом тике так, чтобы правый край панели всегда был на правом краю $card (x = 1160 - width). При width=0 панель уходит вправо за край; при width=350 занимает x=810..1160.
  </action>
  <verify>
    <automated>Select-String -Path "E:/Bratsy_DP/app/create_test.ps1" -Pattern '\$settingsTimer' | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - Файл содержит `$settingsPanel = New-Object System.Windows.Forms.Panel`
    - Файл содержит `$settingsTimer = New-Object System.Windows.Forms.Timer`
    - Файл содержит `$settingsTimer.Interval = 12` (тот же интервал, что у CardTimer)
    - Файл содержит `$settingsPanelTargetW = 350`
    - Файл содержит `$saveBtn = New-Object System.Windows.Forms.Button` и текст "Сохранить"
    - Файл содержит `$cancelBtn` с текстом "Отмена" и Add_Click, вызывающим `$settingsTimer.Start()`
    - Файл содержит `$card.Controls.Add($settingsPanel)` — панель добавляется в $card, не в $form
    - Файл содержит `$settingsPanel.Visible = $false` (скрыта при старте)
    - Логика тика: при closing уменьшает Width, при открытии — увеличивает; останавливает таймер при достижении цели
    - НЕТ прямого `$form.Controls.Add($settingsPanel)`
  </acceptance_criteria>
  <done>Боковая панель $settingsPanel (350px) выезжает справа через Timer-анимацию (Interval=12, шаг=10px). Заголовок "Настройки", кнопки "Сохранить" и "Отмена" в футере. Отмена закрывает панель без действий.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Пользователь → UI | Все пути вводятся через диалоги или отображаются из settings.json |
| settings.json → приложение | Файл читается с диска при старте; содержимое попадает в TextBox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | settings.json | accept | Локальный файл заводского ПК; данные — только пути, не credentials. Проверка существования пути при Save достаточна. |
| T-01-02 | Denial of Service | Timer (settingsTimer) | mitigate | Вызов Start() только когда панель не анимируется: проверить `$settingsTimer.Enabled -eq $false` перед Start(), чтобы двойной клик не сломал анимацию. |
</threat_model>

<verification>
Запустить скрипт: `powershell -ExecutionPolicy Bypass -File "E:/Bratsy_DP/app/create_test.ps1"`

Проверить визуально:
1. Окно открывается — в правом углу хедера видна иконка ⚙ (около x=1080, y=22)
2. Клик по ⚙ — боковая панель выезжает справа (анимированно, ~35 тиков по 10px)
3. Панель перекрывает карточки этапов, ширина ~350px, заголовок "Настройки" виден
4. Кнопка "Отмена" — панель уезжает обратно вправо с анимацией
5. Повторный клик ⚙ — открывает снова
6. Окно не изменило размер (остаётся 1200×620)
</verification>

<success_criteria>
- `$gearBtn` добавлен в `$card` на позиции (1080, 22), рисует [char]0x2699 через Add_Paint
- `$settingsPanel` создан, Width=0, Visible=$false при старте
- Timer-анимация открывает панель (0→350px) и закрывает (350→0px) с Interval=12, Step=10
- Кнопка "Отмена" закрывает панель без сохранения
- `$saveBtn` присутствует (логика сохранения — в Plan 02)
- Скрипт запускается без ошибок PowerShell
</success_criteria>

<output>
После завершения создать `.planning/phases/01-nastrojki-i-konfiguraciya/01-01-SUMMARY.md`
</output>
