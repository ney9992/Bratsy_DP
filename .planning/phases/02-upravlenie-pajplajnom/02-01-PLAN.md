---
phase: 02-upravlenie-pajplajnom
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bratsy-tauri/src-tauri/src/lib.rs
  - bratsy-tauri/src-tauri/Cargo.toml
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03

must_haves:
  truths:
    - "Rust принимает команду run_stage и немедленно возвращает управление JS (не блокирует)"
    - "Запущенный Child-процесс хранится в State и может быть убит командой stop_stage"
    - "Rust эмитит события stage-status и stage-log через AppHandle"
    - "При завершении процесса (успех или ошибка) Rust эмитит финальное stage-status событие"
  artifacts:
    - path: "bratsy-tauri/src-tauri/src/lib.rs"
      provides: "ProcessMap State, run_stage async, stop_stage, события"
      contains: "Arc<Mutex<HashMap<String, u32>>>"
    - path: "bratsy-tauri/src-tauri/Cargo.toml"
      provides: "зависимости для async"
      contains: "tauri = { version = \"2\", features = [\"unstable\"] }"
  key_links:
    - from: "run_stage Rust command"
      to: "stage-status JS event"
      via: "app_handle.emit()"
      pattern: "app_handle\\.emit"
    - from: "ProcessMap State"
      to: "stop_stage command"
      via: "Arc<Mutex<HashMap>>"
      pattern: "Arc<Mutex<HashMap"
---

<objective>
Rust-бэкенд: неблокирующий запуск процессов, хранение PID в State, остановка процесса по команде, эмиссия событий stage-status и stage-log в реальном времени.

Purpose: Заложить основу для real-time pipeline control — JS не должен висеть на invoke(), все обновления UI приходят через Tauri events.
Output: Обновлённые lib.rs и Cargo.toml с полным Rust-слоем управления процессами.
</objective>

<execution_context>
@e:/Bratsy_DP/.claude/get-shit-done/workflows/execute-plan.md
@e:/Bratsy_DP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@e:/Bratsy_DP/.planning/ROADMAP.md
@e:/Bratsy_DP/.planning/REQUIREMENTS.md
@e:/Bratsy_DP/.planning/phases/02-upravlenie-pajplajnom/02-CONTEXT.md

<interfaces>
<!-- Текущее состояние lib.rs (извлечено для планирования) -->

Существующие команды (будут изменены/дополнены):
```rust
// ЗАМЕНИТЬ — текущая блокирующая реализация
#[tauri::command]
fn run_stage(stage: String) -> Result<String, String> { ... }

// УБРАТЬ из invoke_handler — заменяем на async версию с events
fn run_full_pipeline() -> Result<String, String> { ... }
```

Текущая структура State — отсутствует (нужно добавить):
```rust
// ДОБАВИТЬ — хранение PID запущенных процессов
use std::collections::HashMap;
use std::sync::{Arc, Mutex};
// ProcessMap: stage_name -> PID (u32)
// Используем PID вместо Child т.к. Child не реализует Send
```

Паттерн emit событий (Tauri v2):
```rust
// app_handle.emit("event-name", payload) — broadcast ко всем окнам
// Payload должен быть Serialize
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProcessMap State + Cargo зависимости</name>
  <files>
    bratsy-tauri/src-tauri/src/lib.rs
    bratsy-tauri/src-tauri/Cargo.toml
  </files>
  <read_first>
    - bratsy-tauri/src-tauri/src/lib.rs (весь файл — уже прочитан в планировании)
    - bratsy-tauri/src-tauri/Cargo.toml (весь файл — уже прочитан в планировании)
  </read_first>
  <action>
**Cargo.toml** — добавить в [dependencies]:
```toml
tauri = { version = "2", features = ["unstable"] }
```
(feature "unstable" нужен для `app_handle.emit()` broadcast в Tauri v2)

**lib.rs** — добавить imports и State-структуру в начало файла:
```rust
use std::collections::HashMap;
use std::sync::{Arc, Mutex};

// ProcessMap хранит PID запущенных процессов: stage_id -> pid
// Используем PID (u32) вместо Child, потому что Child не реализует Send
// и не может быть перемещён в Arc<Mutex<...>> для шаринга между потоками
pub struct ProcessMap(pub Arc<Mutex<HashMap<String, u32>>>);
```

В функцию `run()` добавить `.manage()` ДО `.invoke_handler()`:
```rust
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .manage(ProcessMap(Arc::new(Mutex::new(HashMap::new()))))
        .invoke_handler(tauri::generate_handler![
            get_settings,
            save_settings,
            run_stage,
            stop_stage,
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```
Убрать `run_full_pipeline` из invoke_handler (он будет переосмыслен в Phase 3).
  </action>
  <verify>
    <automated>cd e:\Bratsy_DP\bratsy-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>
    `cargo check` завершается без ошибок.
    lib.rs содержит `pub struct ProcessMap`.
    Cargo.toml содержит `features = ["unstable"]` для tauri.
  </done>
</task>

<task type="auto">
  <name>Task 2: run_stage (async + события) и stop_stage</name>
  <files>
    bratsy-tauri/src-tauri/src/lib.rs
  </files>
  <read_first>
    - bratsy-tauri/src-tauri/src/lib.rs (прочитан в Task 1)
  </read_first>
  <action>
Заменить текущий `run_stage` на async версию и добавить `stop_stage`.

**Payload-структуры** (добавить перед командами):
```rust
#[derive(Serialize, Clone)]
pub struct StageStatusPayload {
    pub stage: String,
    pub status: String,   // "running" | "done" | "error"
}

#[derive(Serialize, Clone)]
pub struct StageLogPayload {
    pub stage: String,
    pub line: String,
}
```

**run_stage** — полная замена:
```rust
#[tauri::command]
async fn run_stage(
    stage: String,
    state: tauri::State<'_, ProcessMap>,
    app_handle: tauri::AppHandle,
) -> Result<(), String> {
    // Эмитим "running" сразу
    let _ = app_handle.emit("stage-status", StageStatusPayload {
        stage: stage.clone(),
        status: "running".to_string(),
    });

    // Имитация скрипта — в Phase 3 заменится реальным PowerShell-вызовом
    let script = format!(
        "for ($i=1; $i -le 5; $i++) {{ Write-Output \"[{stage}] step $i/5\"; Start-Sleep -Milliseconds 400 }}; Write-Output 'done'",
        stage = stage
    );

    use std::process::{Command, Stdio};
    let mut child = Command::new("powershell")
        .args(["-ExecutionPolicy", "Bypass", "-Command", &script])
        .stdout(Stdio::piped())
        .stderr(Stdio::piped())
        .spawn()
        .map_err(|e| e.to_string())?;

    let pid = child.id();

    // Сохраняем PID в State
    {
        let mut map = state.0.lock().unwrap();
        map.insert(stage.clone(), pid);
    }

    // Клонируем нужное для async блока
    let stage_clone = stage.clone();
    let app_clone = app_handle.clone();
    let state_arc = state.0.clone();

    // Читаем stdout построчно в отдельном потоке (std::thread — не async,
    // т.к. BufReader::lines() блокирующий)
    tauri::async_runtime::spawn_blocking(move || {
        use std::io::{BufRead, BufReader};

        if let Some(stdout) = child.stdout.take() {
            let reader = BufReader::new(stdout);
            for line in reader.lines() {
                match line {
                    Ok(l) => {
                        let _ = app_clone.emit("stage-log", StageLogPayload {
                            stage: stage_clone.clone(),
                            line: l,
                        });
                    }
                    Err(_) => break,
                }
            }
        }

        let status = child.wait().unwrap_or_else(|_| {
            std::process::ExitStatus::from_raw(1)
        });

        // Убираем из State
        {
            let mut map = state_arc.lock().unwrap();
            map.remove(&stage_clone);
        }

        let final_status = if status.success() { "done" } else { "error" };
        let _ = app_clone.emit("stage-status", StageStatusPayload {
            stage: stage_clone,
            status: final_status.to_string(),
        });
    });

    Ok(())
}
```

**stop_stage** — новая команда:
```rust
#[tauri::command]
async fn stop_stage(
    stage: String,
    state: tauri::State<'_, ProcessMap>,
    app_handle: tauri::AppHandle,
) -> Result<(), String> {
    let pid = {
        let mut map = state.0.lock().unwrap();
        map.remove(&stage)
    };

    if let Some(pid) = pid {
        // taskkill /F /PID <pid> — принудительное завершение на Windows
        let _ = std::process::Command::new("taskkill")
            .args(["/F", "/PID", &pid.to_string()])
            .output();

        let _ = app_handle.emit("stage-status", StageStatusPayload {
            stage: stage.clone(),
            status: "error".to_string(),   // остановка = error-состояние (красный пилл)
        });

        let _ = app_handle.emit("stage-log", StageLogPayload {
            stage,
            line: "[остановлено пользователем]".to_string(),
        });
    }

    Ok(())
}
```

**ВАЖНО:** Убедись что `use std::process::ExitStatus` доступен — на Windows нужен `std::os::windows::process::ExitStatusExt`. Используй альтернативу: вместо `ExitStatus::from_raw(1)` используй `child.kill().ok(); child.wait().ok()` в блоке ошибки.

Финальная секция imports в начале файла:
```rust
use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::{Arc, Mutex};
use std::fs;
use std::path::PathBuf;
use std::process::Command;
```
  </action>
  <verify>
    <automated>cd e:\Bratsy_DP\bratsy-tauri && cargo build 2>&1 | grep -E "^error" | head -20</automated>
  </verify>
  <done>
    `cargo build` завершается без ошибок (warnings допустимы).
    lib.rs содержит `async fn run_stage`.
    lib.rs содержит `fn stop_stage`.
    lib.rs содержит `app_handle.emit("stage-status"`.
    lib.rs содержит `app_handle.emit("stage-log"`.
    lib.rs содержит `tauri::async_runtime::spawn_blocking`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS → Rust invoke | stage ID приходит из JS, используется в shell-команде |
| Rust → OS process | команда powershell выполняется в системе |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Injection | run_stage: stage param в PowerShell script | mitigate | stage ID берётся из data-stage атрибута (фиксированный набор: autocad/pdm/excel/plantsim/report). Добавить allowlist-проверку в начале run_stage: `let allowed = ["autocad","pdm","excel","plantsim","report"]; if !allowed.contains(&stage.as_str()) { return Err("invalid stage".into()); }` |
| T-02-02 | DoS | run_stage: многократный параллельный запуск одного этапа | mitigate | В начале run_stage проверить map.contains_key(&stage) — если уже запущен, вернуть Err("already running") |
| T-02-03 | Elevation | taskkill /F убивает процесс по PID | accept | PID берётся из собственного State, не из внешнего ввода. Риск минимален в заводской среде |
</threat_model>

<verification>
После выполнения обоих tasks:

```bash
cd e:\Bratsy_DP\bratsy-tauri && cargo build --release 2>&1 | tail -10
```

Ожидаемый результат: `Finished release` без `error`.
</verification>

<success_criteria>
- `cargo build` проходит без ошибок
- `run_stage` возвращает `Ok(())` немедленно (не блокирует до завершения процесса)
- ProcessMap инициализирован через `.manage()` в `run()`
- `stop_stage` зарегистрирован в `generate_handler!`
- События `stage-status` и `stage-log` эмитируются через `app_handle.emit()`
</success_criteria>

<output>
После завершения создать `.planning/phases/02-upravlenie-pajplajnom/02-01-SUMMARY.md`
</output>
