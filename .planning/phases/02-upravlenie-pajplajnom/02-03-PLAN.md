---
phase: 02-upravlenie-pajplajnom
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - bratsy-tauri/src/styles.css
autonomous: true
requirements:
  - PIPE-01
  - UI-01
  - UI-03

must_haves:
  truths:
    - "Пилл pill-running (синий) отображается когда этап запущен"
    - "Пилл pill-done (зелёный) отображается при успешном завершении"
    - "Пилл pill-error (красный) отображается при ошибке или остановке"
    - "Лог-панель скрыта по умолчанию, появляется плавно при запуске"
    - "Toast появляется снизу-вверх в правом нижнем углу и исчезает через 4 сек"
    - "Активная карточка при hover показывает красную рамку (подсказка: клик = стоп)"
  artifacts:
    - path: "bratsy-tauri/src/styles.css"
      provides: "CSS для pill-running/done/error, log-panel, toast"
      contains: ".pill-running"
  key_links:
    - from: ".stage-card.stage-running:hover"
      to: "border-color: var(--red)"
      via: "CSS hover selector"
      pattern: "stage-running.*hover"
    - from: ".log-panel"
      to: "height: 0 / height: 130px transition"
      via: "CSS transition + .visible class"
      pattern: "log-panel.*transition"
---

<objective>
CSS-слой: стили лог-панели, toast-уведомлений, новых классов пилл и hover-состояния активной карточки (stage-running).

Purpose: Визуально оформить все новые элементы UI в соответствии с locked decisions (D-01..D-09) и существующей цветовой схемой приложения.
Output: Обновлённый styles.css с полным набором визуальных состояний pipeline UI.
</objective>

<execution_context>
@e:/Bratsy_DP/.claude/get-shit-done/workflows/execute-plan.md
@e:/Bratsy_DP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@e:/Bratsy_DP/.planning/phases/02-upravlenie-pajplajnom/02-CONTEXT.md

<interfaces>
<!-- Существующие CSS переменные из styles.css (извлечено) -->
```css
:root {
  --blue: #1761A5;
  --blue-light: #EBF4FF;
  --green: #0F6E56;
  --green-light: #EDF7F0;
  --red: #C0392B;
  --red-light: #FEF3F2;
  --gray: #8E8E93;
  --gray-light: #F2F2F7;
  --text: #1C1C1E;
  --text-sec: #6C6C70;
  --border: #E5E5EA;
}
```

Существующие pill классы (расширяем, не меняем):
```css
.pill-ready  { background: var(--green-light); color: var(--green); }
.pill-active { background: var(--blue-light);  color: var(--blue); }
.pill-idle   { background: var(--gray-light);  color: var(--gray); }
```

Существующий stage-card hover (оставить, добавить исключение для stage-running):
```css
.stage-card:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(0,0,0,0.10);
  border-color: #D0D0D8;
}
.stage-card.stage-active {
  border-color: var(--blue);
  border-width: 2px;
}
```

Locked decisions (из CONTEXT.md):
- D-01: лог ~130px, тёмный фон, monospace
- D-02: лог скрыт по умолчанию, CSS transition height 0→130px
- D-03: заголовок лога с именем этапа
- D-04: строки снизу вверх, auto-scroll, timestamp
- D-05: toast правый нижний угол, 300px, border-radius 12px, 4 сек
- D-06: зелёный = done (#34C759), красный = error (#FF3B30)
- D-09: при hover на активную карточку → border красный
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Новые pill классы и stage-running состояние</name>
  <files>
    bratsy-tauri/src/styles.css
  </files>
  <read_first>
    - bratsy-tauri/src/styles.css (прочитан в планировании — структура известна)
  </read_first>
  <action>
Добавить в конец секции `/* Pills */` (после строки `.pill-idle { ... }`) новые классы:

```css
.pill-running { background: var(--blue-light);  color: var(--blue); }
.pill-done    { background: var(--green-light); color: var(--green); }
.pill-error   { background: var(--red-light);   color: var(--red); }
```

Добавить новую точку `dot-red` в секции `/* Status dots */` (после `.dot-gray`):

```css
.dot-red { background: #FF3B30; }
```

Добавить состояние `stage-running` после блока `.stage-card.stage-active { ... }`:

```css
/* Активный этап (запущен) — синяя рамка */
.stage-card.stage-running {
  border-color: var(--blue);
  border-width: 2px;
}

/* При hover на запущенный этап — красная рамка (подсказка: клик = стоп) */
.stage-card.stage-running:hover {
  border-color: var(--red);
  box-shadow: 0 8px 24px rgba(192, 57, 43, 0.15);
  transform: translateY(-3px);
}
```
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'e:\Bratsy_DP\bratsy-tauri\src\styles.css' -Pattern '\.pill-running','\.pill-done','\.pill-error','\.dot-red','stage-running' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <done>
    styles.css содержит `.pill-running`, `.pill-done`, `.pill-error`, `.dot-red`, `.stage-card.stage-running`, `.stage-card.stage-running:hover`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Лог-панель и toast CSS</name>
  <files>
    bratsy-tauri/src/styles.css
  </files>
  <read_first>
    - bratsy-tauri/src/styles.css (уже прочитан — дополнительное чтение не нужно)
  </read_first>
  <action>
Добавить в конец файла styles.css (после последнего правила `.btn-cancel:hover`) два новых блока.

**Блок 1 — Лог-панель** (per D-01, D-02, D-03, D-04):

```css
/* ── Log panel ── */
.log-panel {
  overflow: hidden;
  max-height: 0;
  transition: max-height 0.35s cubic-bezier(0.4, 0, 0.2, 1),
              opacity 0.35s ease;
  opacity: 0;
  margin: 0 24px;
  border-radius: 14px;
  margin-bottom: 0;
}

.log-panel.visible {
  max-height: 162px;   /* header 32px + body 130px */
  opacity: 1;
  margin-bottom: 12px;
}

.log-header {
  display: flex;
  align-items: center;
  gap: 8px;
  background: #1C1C1E;
  border-radius: 14px 14px 0 0;
  padding: 7px 14px 6px;
  border-bottom: 1px solid rgba(255,255,255,0.08);
}

.log-dot {
  display: inline-block;
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #34C759;
  flex-shrink: 0;
}

.log-title {
  font-size: 12px;
  font-weight: 500;
  color: #8E8E93;
  letter-spacing: 0.2px;
}

.log-body {
  height: 130px;
  background: #1C1C1E;
  border-radius: 0 0 14px 14px;
  overflow-y: auto;
  padding: 8px 14px;
  font-family: Menlo, Consolas, "Courier New", monospace;
  font-size: 12px;
  color: #E5E5EA;
  line-height: 1.6;
  /* Скроллбар в стиле macOS */
  scrollbar-width: thin;
  scrollbar-color: rgba(255,255,255,0.2) transparent;
}

.log-body::-webkit-scrollbar { width: 5px; }
.log-body::-webkit-scrollbar-track { background: transparent; }
.log-body::-webkit-scrollbar-thumb {
  background: rgba(255,255,255,0.2);
  border-radius: 3px;
}

.log-line {
  display: flex;
  gap: 10px;
  margin-bottom: 1px;
}

.log-ts {
  color: #636366;
  flex-shrink: 0;
  user-select: none;
}

.log-text {
  color: #E5E5EA;
  word-break: break-all;
}
```

**Блок 2 — Toast уведомления** (per D-05, D-06, D-07):

```css
/* ── Toast notifications ── */
.toast-container {
  position: fixed;
  bottom: 24px;
  right: 24px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  z-index: 100;
  pointer-events: none;
}

.toast {
  width: 300px;
  padding: 14px 18px;
  border-radius: 12px;
  font-size: 13.5px;
  font-weight: 500;
  font-family: inherit;
  color: #fff;
  box-shadow: 0 4px 20px rgba(0,0,0,0.18);
  /* Начальное состояние: снизу и прозрачный */
  transform: translateY(20px);
  opacity: 0;
  transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1),
              opacity 0.25s ease;
  pointer-events: auto;
}

.toast-done  { background: #0F6E56; }
.toast-error { background: #C0392B; }

.toast-visible {
  transform: translateY(0);
  opacity: 1;
}

.toast-hiding {
  transform: translateY(-8px);
  opacity: 0;
  transition: transform 0.3s ease,
              opacity 0.3s ease;
}
```
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'e:\Bratsy_DP\bratsy-tauri\src\styles.css' -Pattern '\.log-panel','\.log-body','\.log-line','\.toast-container','\.toast-visible','\.toast-hiding' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <done>
    styles.css содержит `.log-panel`, `.log-panel.visible`, `.log-body`, `.log-line`, `.log-ts`, `.toast-container`, `.toast`, `.toast-done`, `.toast-error`, `.toast-visible`, `.toast-hiding`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS-only план | нет исполняемого кода, нет trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | — | styles.css | accept | CSS не создаёт угроз безопасности. XSS-защита реализована в JS (escapeHtml в Plan 02). |
</threat_model>

<verification>
Визуальная проверка после `cargo tauri dev`:

1. Кликнуть на любую карточку — должна появиться лог-панель с плавной анимацией (0.35s)
2. Карточка получает синюю рамку, при hover рамка краснеет
3. Пилл показывает "Запущен" (синий) → "Завершён" (зелёный)
4. Toast появляется снизу-вверх в правом нижнем углу, зелёный при success, исчезает через 4 сек
5. После исчезновения toast лог-панель скрывается через 3 сек
</verification>

<success_criteria>
- styles.css содержит все три pill класса: `.pill-running`, `.pill-done`, `.pill-error`
- styles.css содержит `.stage-card.stage-running:hover { border-color: var(--red) }`
- styles.css содержит `.log-panel` с `transition: max-height`
- styles.css содержит `.log-panel.visible { max-height: 162px }`
- styles.css содержит `.toast-container` с `position: fixed; bottom: 24px; right: 24px`
- styles.css содержит `.toast-visible` и `.toast-hiding` с transition
</success_criteria>

<output>
После завершения создать `.planning/phases/02-upravlenie-pajplajnom/02-03-SUMMARY.md`
</output>
