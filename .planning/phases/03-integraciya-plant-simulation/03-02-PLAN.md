---
phase: 03-integraciya-plant-simulation
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - bratsy-tauri/src/index.html
  - bratsy-tauri/src/main.js
  - bratsy-tauri/src/styles.css
autonomous: true
requirements:
  - INT-01
  - INT-02
  - PIPE-04

must_haves:
  truths:
    - "В панели настроек появились 4-е и 5-е поля: PlantSimulation.exe и .spm макрос"
    - "Сохранение настроек передаёт plant_sim_exe и plant_sim_macro в Rust"
    - "Загрузка настроек заполняет поля inputPlantSimExe и inputPlantSimMacro"
    - "Панель результатов (#resultsPanel) скрыта по умолчанию"
    - "После получения stage-results панель появляется с CSS-анимацией"
    - "Три карточки показывают числа: #resLoad (%), #resThroughput (ед./ч), #resCycleTime (сек)"
    - "При ошибке с префиксом 'config:' в catch вызывается showConfigError() с диалогом"
    - "Диалог ошибки конфигурации предлагает открыть настройки через openSettings()"
  artifacts:
    - path: "bratsy-tauri/src/index.html"
      provides: "2 новых field-group + панель результатов"
      contains: "id=\"resultsPanel\""
    - path: "bratsy-tauri/src/index.html"
      contains: "id=\"resLoad\""
    - path: "bratsy-tauri/src/index.html"
      contains: "id=\"inputPlantSimExe\""
    - path: "bratsy-tauri/src/main.js"
      provides: "listen stage-results + showResultsPanel + showConfigError"
      contains: "listen('stage-results'"
    - path: "bratsy-tauri/src/main.js"
      contains: "showConfigError"
    - path: "bratsy-tauri/src/styles.css"
      provides: "CSS для .results-panel с max-height transition"
      contains: ".results-panel.visible"
  key_links:
    - from: "listen('stage-results')"
      to: "#resLoad, #resThroughput, #resCycleTime"
      via: "document.getElementById('resLoad').textContent = load.toFixed(1)"
      pattern: "resLoad"
    - from: "catch(e) в invoke('run_stage')"
      to: "showConfigError()"
      via: "e.startsWith('config:')"
      pattern: "startsWith.*config"
    - from: "btnSave click handler"
      to: "invoke('save_settings')"
      via: "plant_sim_exe: plantSimExe"
      pattern: "plant_sim_exe"
---

<objective>
Реализовать всю фронтенд-часть интеграции PlantSim: 2 новых поля в панели настроек, панель результатов с тремя карточками, JS-обработчики событий stage-results, диалог ошибки конфигурации.

Purpose: Закрыть PIPE-04 (отображение результатов) и фронтенд-часть INT-01/INT-02 — после этого плана пользователь видит реальные числа из симуляции.
Output: Модифицированные `index.html`, `main.js`, `styles.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-integraciya-plant-simulation/03-CONTEXT.md
@.planning/phases/03-integraciya-plant-simulation/03-PATTERNS.md
@.planning/phases/03-integraciya-plant-simulation/03-01-SUMMARY.md

<interfaces>
<!-- ТЕКУЩИЙ main.js — ключевые участки для модификации (верифицировано в коде) -->

```javascript
// Строки 63-68: ТЕКУЩИЙ catch в invoke('run_stage') — ЗАМЕНИТЬ:
} catch (e) {
    failedAttempts++;
    console.error('run_stage error:', e);
    updatePill(stage, 'error');
    showToast(STAGE_LABELS[stage] || stage, 'error');
}

// Строка 132: showLogPanel() — ДОБАВИТЬ showResultsPanel() рядом
function showLogPanel(visible) {
  const panel = document.getElementById('logPanel');
  if (visible) panel.classList.add('visible');
  else panel.classList.remove('visible');
}

// Строки 185-207: showToast() — ДОБАВИТЬ showConfigError() после

// Строки 258-260: чтение полей (ДОБАВИТЬ plantSimExe, plantSimMacro)
const plantSim = document.getElementById('inputPlantSim').value;
const workDir  = document.getElementById('inputWorkDir').value;
const scripts  = document.getElementById('inputScripts').value;

// Строки 264-273: массив валидации (ДОБАВИТЬ 2 строки)
[[plantSim, 'inputPlantSim', 'errPlantSim'],
 [workDir,  'inputWorkDir',  'errWorkDir'],
 [scripts,  'inputScripts',  'errScripts']].forEach(...)

// Строки 278-284: invoke save_settings — объект settings (ДОБАВИТЬ 2 поля)
settings: {
    plant_sim_path: plantSim,
    work_dir: workDir,
    scripts_dir: scripts,
}

// Строки 331-334: загрузка настроек в DOMContentLoaded (ДОБАВИТЬ 2 строки)
if (s.plant_sim_path) document.getElementById('inputPlantSim').value = s.plant_sim_path;
if (s.work_dir)       document.getElementById('inputWorkDir').value  = s.work_dir;
if (s.scripts_dir)    document.getElementById('inputScripts').value  = s.scripts_dir;

// Строки 309-327: DOMContentLoaded с listen() — ДОБАВИТЬ listen('stage-results')
await listen('stage-status', (event) => { ... });
await listen('stage-log', (event) => { ... });
// ДОБАВИТЬ ЗДЕСЬ
```

<!-- ТЕКУЩИЙ index.html — ключевые участки (верифицировано в коде) -->
```html
<!-- Строки 164-189: существующие .field-group (ДОБАВИТЬ 2 новых после) -->
<!-- Строки 136-143: #logPanel (ДОБАВИТЬ #resultsPanel после) -->
```

<!-- ТЕКУЩИЙ styles.css — ключевой участок (верифицировано в коде) -->
```css
/* Строки 417-433: .log-panel / .log-panel.visible (ДОБАВИТЬ .results-panel аналогично) */
.log-panel { overflow: hidden; max-height: 0; opacity: 0; transition: max-height 0.35s ...; }
.log-panel.visible { max-height: 162px; opacity: 1; margin-bottom: 12px; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML — 2 новых поля настроек + панель результатов</name>
  <files>bratsy-tauri/src/index.html</files>

  <read_first>
    - bratsy-tauri/src/index.html (ПОЛНЫЙ файл — нужно знать точное расположение field-group и logPanel)
    - .planning/phases/03-integraciya-plant-simulation/03-PATTERNS.md (Паттерн 6, Паттерн 7)
    - .planning/phases/03-integraciya-plant-simulation/03-CONTEXT.md (D-10, D-11, D-12)
  </read_first>

  <action>
Внести два изменения в index.html:

**Изменение 1 — Добавить 2 новых field-group в панели настроек:**

Найти третий блок `<div class="field-group">` в секции настроек (поле `inputScripts`). Добавить ПОСЛЕ закрывающего `</div>` этого блока:

```html
<!-- 4-е поле настроек: PlantSimulation.exe (D-02) -->
<div class="field-group">
  <label class="field-label">Путь к PlantSimulation.exe</label>
  <div class="field-row">
    <input class="field-input" id="inputPlantSimExe" type="text" readonly placeholder="Не задан">
    <button class="browse-btn" data-target="inputPlantSimExe" data-type="file">…</button>
  </div>
  <div class="field-error" id="errPlantSimExe">Путь не найден</div>
</div>

<!-- 5-е поле настроек: SimTalk макрос .spm (D-03) -->
<div class="field-group">
  <label class="field-label">Путь к макросу SimTalk (.spm)</label>
  <div class="field-row">
    <input class="field-input" id="inputPlantSimMacro" type="text" readonly placeholder="Не задан">
    <button class="browse-btn" data-target="inputPlantSimMacro" data-type="file">…</button>
  </div>
  <div class="field-error" id="errPlantSimMacro">Путь не найден</div>
</div>
```

Конвенция именования строго соблюдена: `id="input{Name}"` + `id="err{Name}"` — как у существующих полей (inputPlantSim/errPlantSim, inputWorkDir/errWorkDir, inputScripts/errScripts).

**Изменение 2 — Добавить панель результатов после #logPanel (D-10, D-11, D-12):**

Найти закрывающий `</div>` блока `<div class="log-panel" id="logPanel">`. Добавить ПОСЛЕ него, перед `<!-- Footer -->` (или следующей секцией):

```html
<!-- Панель результатов симуляции (Phase 3) — D-10, D-11, D-12 -->
<div class="results-panel" id="resultsPanel">
  <div class="results-header">
    <span class="results-title">Результаты симуляции</span>
  </div>
  <div class="results-body">
    <div class="metric-card">
      <div class="metric-info">
        <div class="metric-label">Загрузка линии</div>
        <div class="metric-value" id="resLoad">—</div>
        <div class="metric-unit">%</div>
      </div>
      <div class="metric-icon" style="background:#EDF7F0">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#0F6E56" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 7 13.5 15.5 8.5 10.5 2 17"/><polyline points="16 7 22 7 22 13"/></svg>
      </div>
    </div>
    <div class="metric-card">
      <div class="metric-info">
        <div class="metric-label">Пропускная способность</div>
        <div class="metric-value" id="resThroughput">—</div>
        <div class="metric-unit">ед./ч</div>
      </div>
      <div class="metric-icon" style="background:#EBF4FF">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#1761A5" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
      </div>
    </div>
    <div class="metric-card">
      <div class="metric-info">
        <div class="metric-label">Время цикла</div>
        <div class="metric-value" id="resCycleTime">—</div>
        <div class="metric-unit">сек</div>
      </div>
      <div class="metric-icon" style="background:#F5F5F7">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#666" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
      </div>
    </div>
  </div>
</div>
```

Панель использует существующий `.metric-card` паттерн (уже стилизован в styles.css). `.results-panel` без класса `.visible` — скрыта по умолчанию через CSS (Task 3 добавит стили).
  </action>

  <verify>
    <automated>
      Select-String -Path bratsy-tauri/src/index.html -Pattern "id=""resultsPanel"""
      Select-String -Path bratsy-tauri/src/index.html -Pattern "id=""resLoad"""
      Select-String -Path bratsy-tauri/src/index.html -Pattern "id=""resThroughput"""
      Select-String -Path bratsy-tauri/src/index.html -Pattern "id=""resCycleTime"""
      Select-String -Path bratsy-tauri/src/index.html -Pattern "id=""inputPlantSimExe"""
      Select-String -Path bratsy-tauri/src/index.html -Pattern "id=""inputPlantSimMacro"""
      Select-String -Path bratsy-tauri/src/index.html -Pattern "id=""errPlantSimExe"""
      Select-String -Path bratsy-tauri/src/index.html -Pattern "id=""errPlantSimMacro"""
    </automated>
  </verify>

  <acceptance_criteria>
    - `index.html` содержит `id="resultsPanel"`
    - `index.html` содержит `id="resLoad"`
    - `index.html` содержит `id="resThroughput"`
    - `index.html` содержит `id="resCycleTime"`
    - `index.html` содержит `id="inputPlantSimExe"`
    - `index.html` содержит `id="inputPlantSimMacro"`
    - `index.html` содержит `id="errPlantSimExe"`
    - `index.html` содержит `id="errPlantSimMacro"`
    - `index.html` содержит `class="results-panel"` (без класса visible — скрыта по умолчанию)
    - `index.html` содержит `class="results-header"` и `class="results-body"`
    - `index.html` содержит `class="metric-unit"` (3 раза — для каждой карточки)
    - Существующие 3 поля (inputPlantSim, inputWorkDir, inputScripts) по-прежнему присутствуют
  </acceptance_criteria>

  <done>index.html содержит 5 полей настроек и панель результатов с 3 metric-card карточками</done>
</task>

<task type="auto">
  <name>Task 2: JS — listen stage-results, showResultsPanel, showConfigError, расширение save/load настроек</name>
  <files>bratsy-tauri/src/main.js</files>

  <read_first>
    - bratsy-tauri/src/main.js (ПОЛНЫЙ файл — нужны точные строки для всех 5 изменений)
    - .planning/phases/03-integraciya-plant-simulation/03-PATTERNS.md (Паттерны 8, 9, 10, 11, 12)
    - .planning/phases/03-integraciya-plant-simulation/03-CONTEXT.md (D-13, D-14, D-15)
  </read_first>

  <action>
Внести 5 точечных изменений в main.js. Читать файл полностью перед правками — строки могут сдвинуться после Task 1 HTML-изменений (только js файл, не html).

**Изменение 1 — ЗАМЕНИТЬ catch в invoke('run_stage') (строки ~63-68):**

НАЙТИ блок:
```javascript
      } catch (e) {
        failedAttempts++;
        console.error('run_stage error:', e);
        updatePill(stage, 'error');
        showToast(STAGE_LABELS[stage] || stage, 'error');
      }
```

ЗАМЕНИТЬ на (D-13 vs D-14 разветвление):
```javascript
      } catch (e) {
        if (typeof e === 'string' && e.startsWith('config:')) {
          // D-13: ошибка конфигурации — диалог с кнопкой «Открыть настройки»
          showConfigError(e.replace('config: ', ''));
        } else {
          // D-14: ошибка выполнения — стандартный механизм Phase 2
          failedAttempts++;
          console.error('run_stage error:', e);
          updatePill(stage, 'error');
          showToast(STAGE_LABELS[stage] || stage, 'error');
        }
      }
```

**Изменение 2 — ДОБАВИТЬ showResultsPanel() после showLogPanel() (~строка 139):**

НАЙТИ функцию `showLogPanel`. Добавить ПОСЛЕ её закрывающей скобки `}`:
```javascript
function showResultsPanel(visible) {
  const panel = document.getElementById('resultsPanel');
  if (!panel) return;
  if (visible) {
    panel.classList.add('visible');
  } else {
    panel.classList.remove('visible');
  }
}
```

**Изменение 3 — ДОБАВИТЬ showConfigError() после showToast() (~строка 207):**

НАЙТИ функцию `showToast`. Добавить ПОСЛЕ её закрывающей скобки `}`:
```javascript
function showConfigError(message) {
  // D-15: confirm() с предложением открыть настройки
  const goSettings = confirm(
    `Ошибка конфигурации Plant Simulation:\n${message}\n\nОткрыть настройки?`
  );
  if (goSettings) openSettings();
}
```

**Изменение 4 — РАСШИРИТЬ save_settings click handler (~строки 257-289):**

НАЙТИ блок чтения полей:
```javascript
  const plantSim = document.getElementById('inputPlantSim').value;
  const workDir  = document.getElementById('inputWorkDir').value;
  const scripts  = document.getElementById('inputScripts').value;
```

ДОБАВИТЬ сразу после (2 новые переменные):
```javascript
  const plantSimExe   = document.getElementById('inputPlantSimExe').value;
  const plantSimMacro = document.getElementById('inputPlantSimMacro').value;
```

НАЙТИ массив валидации:
```javascript
  [[plantSim, 'inputPlantSim', 'errPlantSim'],
   [workDir,  'inputWorkDir',  'errWorkDir'],
   [scripts,  'inputScripts',  'errScripts']].forEach(
```

ЗАМЕНИТЬ на (добавить 2 строки в массив):
```javascript
  [[plantSim,      'inputPlantSim',      'errPlantSim'],
   [workDir,       'inputWorkDir',       'errWorkDir'],
   [scripts,       'inputScripts',       'errScripts'],
   [plantSimExe,   'inputPlantSimExe',   'errPlantSimExe'],
   [plantSimMacro, 'inputPlantSimMacro', 'errPlantSimMacro']].forEach(
```

НАЙТИ объект settings в invoke('save_settings'):
```javascript
      settings: {
        plant_sim_path: plantSim,
        work_dir: workDir,
        scripts_dir: scripts,
      }
```

ЗАМЕНИТЬ на (добавить 2 поля):
```javascript
      settings: {
        plant_sim_path: plantSim,
        work_dir: workDir,
        scripts_dir: scripts,
        plant_sim_exe:   plantSimExe,
        plant_sim_macro: plantSimMacro,
      }
```

Имена ключей `plant_sim_exe` и `plant_sim_macro` ДОЛЖНЫ точно соответствовать полям Rust Settings struct из плана 03-01 (snake_case, Tauri автоматически конвертирует camelCase↔snake_case при сериализации, но здесь используем snake_case напрямую).

**Изменение 5 — РАСШИРИТЬ загрузку настроек в DOMContentLoaded (~строки 331-334):**

НАЙТИ блок загрузки настроек:
```javascript
    if (s.plant_sim_path) document.getElementById('inputPlantSim').value = s.plant_sim_path;
    if (s.work_dir)       document.getElementById('inputWorkDir').value  = s.work_dir;
    if (s.scripts_dir)    document.getElementById('inputScripts').value  = s.scripts_dir;
```

ДОБАВИТЬ сразу после (2 новые строки):
```javascript
    if (s.plant_sim_exe)   document.getElementById('inputPlantSimExe').value   = s.plant_sim_exe;
    if (s.plant_sim_macro) document.getElementById('inputPlantSimMacro').value = s.plant_sim_macro;
```

**Изменение 6 — ДОБАВИТЬ listen('stage-results') в DOMContentLoaded, после listen('stage-log'):**

НАЙТИ в DOMContentLoaded блок:
```javascript
  await listen('stage-log', (event) => {
    const { stage, line } = event.payload;
    appendLog(stage, line);
  });
```

ДОБАВИТЬ после него:
```javascript
  // D-12: панель результатов — появляется при получении stage-results
  await listen('stage-results', (event) => {
    const { stage, load, throughput, cycle_time } = event.payload;
    if (stage !== 'plantsim') return;

    document.getElementById('resLoad').textContent = load.toFixed(1);
    document.getElementById('resThroughput').textContent = throughput.toFixed(0);
    document.getElementById('resCycleTime').textContent = cycle_time.toFixed(1);

    showResultsPanel(true);
  });
```

Поведение при повторном запуске (Claude's Discretion): числа обновляются на месте без скрытия панели — `showResultsPanel(true)` на уже видимой панели добавляет класс `.visible` повторно (idempotent, без визуального сброса).
  </action>

  <verify>
    <automated>
      Select-String -Path bratsy-tauri/src/main.js -Pattern "listen\('stage-results'"
      Select-String -Path bratsy-tauri/src/main.js -Pattern "showResultsPanel"
      Select-String -Path bratsy-tauri/src/main.js -Pattern "showConfigError"
      Select-String -Path bratsy-tauri/src/main.js -Pattern "startsWith\('config:'\)"
      Select-String -Path bratsy-tauri/src/main.js -Pattern "plant_sim_exe"
      Select-String -Path bratsy-tauri/src/main.js -Pattern "plant_sim_macro"
      Select-String -Path bratsy-tauri/src/main.js -Pattern "inputPlantSimExe"
      Select-String -Path bratsy-tauri/src/main.js -Pattern "resLoad"
    </automated>
  </verify>

  <acceptance_criteria>
    - `main.js` содержит `listen('stage-results'`
    - `main.js` содержит `function showResultsPanel(`
    - `main.js` содержит `function showConfigError(`
    - `main.js` содержит `e.startsWith('config:')` в catch блоке
    - `main.js` содержит `plant_sim_exe: plantSimExe` в объекте save_settings
    - `main.js` содержит `plant_sim_macro: plantSimMacro`
    - `main.js` содержит `document.getElementById('inputPlantSimExe').value`
    - `main.js` содержит `document.getElementById('inputPlantSimMacro').value`
    - `main.js` содержит `s.plant_sim_exe` в блоке загрузки настроек
    - `main.js` содержит `s.plant_sim_macro` в блоке загрузки настроек
    - `main.js` содержит `resLoad` и `resThroughput` и `resCycleTime` (в listen)
    - `main.js` содержит `load.toFixed(1)` и `cycle_time.toFixed(1)` и `throughput.toFixed(0)`
    - `main.js` содержит `if (goSettings) openSettings()` в showConfigError
    - Существующий функционал (showLogPanel, showToast, updatePill) не нарушен — grep показывает эти функции по-прежнему
  </acceptance_criteria>

  <done>main.js содержит все 6 изменений: разветвлённый catch, showResultsPanel, showConfigError, расширенные save/load настроек, listen('stage-results')</done>
</task>

<task type="auto">
  <name>Task 3: CSS — .results-panel с max-height transition + .metric-unit</name>
  <files>bratsy-tauri/src/styles.css</files>

  <read_first>
    - bratsy-tauri/src/styles.css (строки 410-435 — .log-panel паттерн для копирования; конец файла — место добавления)
    - .planning/phases/03-integraciya-plant-simulation/03-PATTERNS.md (Паттерн 13)
    - .planning/phases/03-integraciya-plant-simulation/03-RESEARCH.md (Pattern 6, Anti-Patterns: height:auto)
  </read_first>

  <action>
Добавить в КОНЕЦ файла styles.css следующие стили. НЕ изменять существующие стили — только добавить в конец.

```css
/* ── Results panel (Phase 3) ─────────────────────────────────── */
/* Паттерн аналогичен .log-panel (строки 417-433): max-height transition, не height:auto */
.results-panel {
  overflow: hidden;
  max-height: 0;
  transition: max-height 0.35s cubic-bezier(0.4, 0, 0.2, 1),
              opacity 0.35s ease;
  opacity: 0;
  margin: 0 24px;
  border-radius: 14px;
  margin-bottom: 0;
  background: var(--gray-light);
}

.results-panel.visible {
  max-height: 160px;
  opacity: 1;
  margin-bottom: 12px;
}

.results-header {
  padding: 8px 16px 6px;
  font-size: 11px;
  font-weight: 600;
  color: var(--text-sec);
  letter-spacing: 0.5px;
  text-transform: uppercase;
}

.results-body {
  display: flex;
  gap: 12px;
  padding: 0 12px 12px;
}

/* .metric-unit — единицы измерения под числом в карточке */
.metric-unit {
  font-size: 11px;
  color: var(--text-sec);
  margin-top: 2px;
}
```

ВАЖНО: `max-height: 160px` — конкретное числовое значение, НЕ `auto`. CSS transition не работает с `height: auto` или `max-height: auto`. Значение подбирается чуть больше реальной высоты панели (3 карточки в ряд ~140px).

ВАЖНО: `background: var(--gray-light)` — визуально отличает панель результатов от лог-панели. CSS-переменная `--gray-light` уже объявлена в начале styles.css (верифицировано для Phase 1-2).

Если `--gray-light` не существует в файле — использовать `#F8F8FA` как fallback значение.
  </action>

  <verify>
    <automated>
      Select-String -Path bratsy-tauri/src/styles.css -Pattern "\.results-panel\b"
      Select-String -Path bratsy-tauri/src/styles.css -Pattern "\.results-panel\.visible"
      Select-String -Path bratsy-tauri/src/styles.css -Pattern "\.results-header"
      Select-String -Path bratsy-tauri/src/styles.css -Pattern "\.results-body"
      Select-String -Path bratsy-tauri/src/styles.css -Pattern "\.metric-unit"
      Select-String -Path bratsy-tauri/src/styles.css -Pattern "max-height: 160px"
    </automated>
  </verify>

  <acceptance_criteria>
    - `styles.css` содержит `.results-panel {`
    - `styles.css` содержит `.results-panel.visible {`
    - `styles.css` содержит `max-height: 0` в `.results-panel` (скрытое состояние)
    - `styles.css` содержит `max-height: 160px` в `.results-panel.visible` (не `auto`)
    - `styles.css` содержит `opacity: 0` в `.results-panel`
    - `styles.css` содержит `opacity: 1` в `.results-panel.visible`
    - `styles.css` содержит `transition: max-height` в `.results-panel`
    - `styles.css` содержит `.results-header {`
    - `styles.css` содержит `.results-body {`
    - `styles.css` содержит `.metric-unit {`
    - Существующие стили `.log-panel` и `.log-panel.visible` не изменены (grep показывает их по-прежнему)
  </acceptance_criteria>

  <done>styles.css содержит .results-panel с max-height transition, .results-header, .results-body, .metric-unit — панель результатов визуально готова</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke-тест — полный happy path с mock-plantsim</name>

  <what-built>
Реализован полный стек интеграции PlantSim:
- Rust backend: валидация путей, Command::new(plant_sim_exe), results.txt parsing, stage-results event
- JS: listen('stage-results'), showResultsPanel(), showConfigError(), расширенные настройки
- HTML: 5 полей настроек, панель результатов с 3 карточками
- CSS: .results-panel с max-height transition
- mock-plantsim.ps1: пишет results.txt (load=87.3, throughput=42, cycle_time=18.5)
  </what-built>

  <how-to-verify>
Выполнить в браузере приложения (`npm run tauri dev` из bratsy-tauri/):

**Проверка 1 — Настройки (D-02, D-03):**
1. Открыть настройки (кнопка ⚙)
2. Убедиться что появились поля "Путь к PlantSimulation.exe" и "Путь к макросу SimTalk (.spm)"
3. Ввести в поле PlantSimulation.exe: путь к powershell.exe (`C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`)
4. Ввести любые пути в .spm и .spp (создать пустые файлы если нужно)
5. Сохранить → настройки сохраняются, панель закрывается
6. Открыть снова → значения сохранились

**Проверка 2 — Ошибка конфигурации (D-13):**
1. Очистить поле PlantSimulation.exe в настройках
2. Сохранить
3. Нажать на карточку PlantSim в пайплайне
4. Должен появиться диалог: "Ошибка конфигурации Plant Simulation: PlantSimulation.exe не найден..."
5. Нажать OK/Cancel — краша нет
6. Нажать "Открыть настройки" в диалоге — панель настроек открывается

**Проверка 3 — Happy path с mock (INT-01, INT-02, PIPE-04):**
1. В настройках указать plant_sim_exe = путь к `bratsy-tauri/dev-tools/mock-plantsim.ps1`
   (временно — для теста; настоящий запуск через powershell -File)
   ЛИБО: вручную вызвать mock-plantsim.ps1, создать results.txt в work_dir, затем запустить PlantSim stage
2. Нажать карточку PlantSim → pill переходит в "Запущен"
3. Лог-панель показывает сообщения от mock
4. После завершения: pill "Завершён", появляется панель результатов
5. Панель содержит: Загрузка линии 87.3%, Пропускная способность 42 ед./ч, Время цикла 18.5 сек

**Проверка 4 — Панель результатов (PIPE-04):**
- Панель НЕ видна при загрузке приложения
- Появляется с анимацией после PlantSim завершения
- Числа соответствуют содержимому results.txt
  </how-to-verify>

  <resume-signal>
Введи "approved" если все 4 проверки прошли, или опиши какая проверка не прошла и что увидел.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView JS → Rust invoke | Данные настроек пересекают IPC из WebView |
| Rust stage-results event → WebView JS | Числа из results.txt попадают в DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | inputPlantSimExe — путь задаётся через input | accept | browse-btn использует Tauri file dialog (не свободный ввод); readonly атрибут на input |
| T-02-02 | Spoofing | stage-results payload в WebView | accept | Источник — Rust backend, данные из локального results.txt; локальное приложение без сети |
| T-02-03 | Information Disclosure | confirm() диалог показывает текст ошибки | accept | Текст ошибки — "config: PlantSimulation.exe не найден", не содержит чувствительных данных |
| T-02-04 | Tampering | .metric-unit CSS может конфликтовать | mitigate | .metric-unit объявлен в конце файла как глобальный — если конфликтует с существующим, уточнить селектор до `.results-body .metric-unit` |
</threat_model>

<verification>
После выполнения всех tasks:
1. `Select-String "resultsPanel" bratsy-tauri/src/index.html` → находит
2. `Select-String "listen.*stage-results" bratsy-tauri/src/main.js` → находит
3. `Select-String "showConfigError" bratsy-tauri/src/main.js` → находит (функция + вызов)
4. `Select-String "results-panel" bratsy-tauri/src/styles.css` → находит .results-panel и .results-panel.visible
5. `npm run tauri dev` из bratsy-tauri/ → компилируется и запускается
6. Smoke: карточка PlantSim без настроек → диалог ошибки (не краш)
7. Smoke: после stage-results event → панель результатов видна, числа корректны
</verification>

<success_criteria>
- index.html содержит 5 field-group в настройках (включая inputPlantSimExe и inputPlantSimMacro)
- index.html содержит #resultsPanel с 3 карточками (#resLoad, #resThroughput, #resCycleTime)
- main.js содержит listen('stage-results') обновляющий 3 DOM-элемента и вызывающий showResultsPanel(true)
- main.js содержит showConfigError() вызывающий openSettings() при подтверждении
- main.js catch разветвлён: config: → showConfigError, иначе → стандартный toast
- styles.css содержит .results-panel с max-height: 0 → 160px transition
- Smoke-тест: панель результатов появляется после PlantSim завершения с числами из results.txt
- Checkpoint: пользователь подтверждает all 4 проверки
</success_criteria>

<output>
После завершения создать `.planning/phases/03-integraciya-plant-simulation/03-02-SUMMARY.md`
</output>
